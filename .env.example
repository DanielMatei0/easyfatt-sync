# Easyfatt Sync — variabili ambiente (opzionali)
# Copia in .env solo in sviluppo locale. Non committare .env

# Publish release GitHub (solo macchina di build / CI, non nell'app)
# GH_TOKEN=ghp_your_github_personal_access_token

# L'app Electron NON usa RESEND_API_KEY: il supporto passa dall'API Aven Labs.
# Vedi backend-examples/nextjs/.env.example per il server.
