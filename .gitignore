# Dependencies
node_modules/

# Secrets / OAuth (never commit)
oauth_credentials.json
token.json
.env
.env.local
.env.production
.env.*.local

# Build output
dist/
release/
out/
*.dmg
*.exe
*.blockmap
*.AppImage
*.snap
*.deb
*.zip
latest.yml
latest-mac.yml
latest-linux.yml

# Local app data / backups (Easyfatt Sync)
*.easyfatt-sync-backup.json
backups/
*.backup.json

# Sync diff data (local only, contains client data)
profileDataSnapshots.json
syncHistoryDiffs.json

# Logs
*.log
logs/

# macOS icon build temp
assets/icon.iconset/

# OS / editor
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/

# Backend example (keep .env.example only)
backend-examples/**/.env.local

# Safe templates (committed)
# oauth_credentials.example.json
# .env.example

# macOS resource fork files
._*
**/._*
.DS_Store
